import 'package:flutter/material.dart';

/// Spacing and general dimensions
class WidAppDimensions {
  /// Default size for borders
  static double borderWidth = 1.6;

  /// Default radius for controllers
  static double borderRadiusControllers = 12;

  /// Default height for controllers
  static double controllerHeight = 48;

  /// Default spacing gap for widgets
  static double innerGap = 18;

  /// Default spacing gap for cards
  static double cardSmallSpacing = 10;

  /// Default large spacing gap for cards
  static double cardLargeSpacing = cardSmallSpacing * 2.2;

  /// Default page inset
  static double pageInsetGap = 30;

  /// Default animation duration
  static int animationDuration = 200;

  /// Custom app header height
  static double customToolbarHeight = kToolbarHeight * 1.4;
}
